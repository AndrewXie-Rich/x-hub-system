# Whitepaper Submodule (Planned)

Decision (2026-02-12): `White paper/` stays as a **separate git repo** and is included in `x-hub-system/` as a **git submodule**.

Planned mount path in this repo:
- `docs/whitepaper/`

Notes:
- Do not copy/vend the whitepaper contents into this MIT code repository by default.
- When the whitepaper repo URL is ready, add it as a submodule and update links in `X_MEMORY.md`.

